export const isBusy = state => state.processes.busy;
export const isVisible = state => state.processes.visible;
export const getProcesses = state => state.processes.processes;
export const getNotifications = state => state.processes.notifications;
